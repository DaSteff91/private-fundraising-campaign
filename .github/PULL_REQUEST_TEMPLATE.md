## Summary

Briefly describe what this PR changes and why.

## Test plan

- [ ] `npm test` passes
- [ ] `npm run build` passes (local `.env` or copy from `.env.example`)

## Checklist

- [ ] No `.env`, real payee data, or PII proof photos in the diff
- [ ] Notable user-facing / contract changes noted under `[Unreleased]` in `CHANGELOG.md`
- [ ] Breaking env, schema, or PocketBase pin changes called out above
- [ ] Docs updated if behavior or setup changed
